import { TestModal } from "../components";
import { ModalComponentInterface } from "../interface";

export const MODAL_TYPES = {
  TEST_MODEL: "TEST_MODEL",
};

export const MODEL_COMPONENTS: ModalComponentInterface = {
  // [MODAL_TYPES.TEST_MODEL]: TestModal,
};
