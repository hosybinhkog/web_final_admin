import { NextPage } from "next";
import { Button, Checkbox, Input } from "../components";

const Test: NextPage = () => {
  return (
    <div className=''>
      <Checkbox />
      <Button>Button</Button>
      <Input />
    </div>
  );
};

export default Test;
